<UserControl x:Class="Author.Views.ActorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Author.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" Background="White">
        <StackPanel Orientation="Vertical">
            <Label>Character Name</Label>
            <TextBox x:Name="ActorNameControl" MinWidth="200" Background="White" Margin="3" ></TextBox>

            <Label>Character Description</Label>
            <RichTextBox x:Name="CharacterDescriptionControl" MinHeight="200" Margin="3"></RichTextBox>
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <Image x:Name="SketchBoxControl" Width="200" Height="200"></Image>
            <Button>Update Image</Button>
            <Label>Character Notes</Label>
            <RichTextBox x:Name="CharacterNotesControl" BorderThickness="1"></RichTextBox>
        </StackPanel>
    </StackPanel>
</UserControl>
